squeue -u $USER -h -o "%i %N" \
| while read -r JOBID NODE; do
    echo "Job $JOBID is running on node $NODE"
    echo -n "GPU type: "
    ssh -n "$NODE" 'nvidia-smi --query-gpu=name --format=csv,noheader'
    echo
  done
